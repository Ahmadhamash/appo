# Migrations

This directory is reserved for reviewed, explicit Prisma migrations. Do not use `prisma db push` for
shared environments. The first migration belongs to Phase 1 and is intentionally absent from the
architecture scaffold.
